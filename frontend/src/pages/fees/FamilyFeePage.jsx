import React, { useEffect, useState } from 'react';import DashboardLayout from '../../layouts/DashboardLayout';import { feeService } from '../../services/feeService';
export default function FamilyFeePage(){const [data,setData]=useState();useEffect(()=>{feeService.family().then(setData)},[]);return <DashboardLayout role="PARENT"><h1 className="text-2xl font-bold">Family fee summary</h1><div className="mt-5 grid gap-4 md:grid-cols-2">{data?.children.map(c=><div key={c.student.id} className="rounded-2xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><p className="font-semibold">{c.student.name}</p><p className="text-sm text-slate-500">{c.student.className} {c.student.section||''}</p><p className="mt-4 text-2xl font-bold">₹{(c.pendingMinor/100).toLocaleString('en-IN')}</p><p className="text-sm text-slate-500">Pending</p></div>)}</div><div className="mt-5 rounded-2xl bg-blue-600 p-5 text-white"><p>Combined family dues</p><p className="text-3xl font-bold">₹{((data?.combinedPendingMinor||0)/100).toLocaleString('en-IN')}</p></div></DashboardLayout>}
