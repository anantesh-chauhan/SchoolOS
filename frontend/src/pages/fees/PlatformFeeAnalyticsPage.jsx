import React,{useEffect,useState}from'react';import DashboardLayout from'../../layouts/DashboardLayout';import{feeService}from'../../services/feeService';
export default function PlatformFeeAnalyticsPage(){const[data,setData]=useState();useEffect(()=>{feeService.platformAnalytics().then(setData)},[]);return <DashboardLayout role="PLATFORM_OWNER"><h1 className="text-2xl font-bold">Fee module analytics</h1><div className="mt-5 grid gap-4 sm:grid-cols-2"><div className="rounded-2xl border p-5 dark:border-slate-800"><p className="text-slate-500">Configured schools</p><p className="text-3xl font-bold">{data?.configuredSchools||0}</p></div><div className="rounded-2xl border p-5 dark:border-slate-800"><p className="text-slate-500">Enabled schools</p><p className="text-3xl font-bold">{data?.enabledSchools||0}</p></div></div><div className="mt-5 space-y-3">{data?.schools.map(x=><div key={x.school.schoolCode} className="rounded-2xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><div className="flex justify-between"><p className="font-semibold">{x.school.schoolName}</p><span>{x.enabled?'Enabled':'Disabled'}</span></div><p className="mt-2 text-sm text-slate-500">{x.allowPlatformSummary?`${x.collection?._count||0} payments · ${(Number(x.collection?._sum?.amountMinor||0)/100).toLocaleString()}`:'Financial summary hidden by school policy'}</p></div>)}</div></DashboardLayout>}
